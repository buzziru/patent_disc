# 다음 세션 인계

> 읽는 순서: `PROJECT.md`(SSOT) → 이 문서. 수치 SSOT는 `output/*.json`.

## 지금 상태

훈련 3회(KoBERT 재현 / A.X@512 / A.X@8192)와 무훈련 분석 3종(`06_01`·`06_02`·`06_03`)이 끝났다. **무훈련으로 짜낼 수 있는 개선은 전부 측정했고 모두 닫혔다** — 다음 이득은 훈련에서 나와야 한다. 분석이 남긴 가장 큰 미회수 헤드룸은 **다라벨 문서의 카디널리티(micro +1.63pt 상한)**이며 회수 경로는 손실 함수다(아래 2단계).

현재 최고 성능은 A.X@8192(exp1): 멀티라벨 micro **0.8685** / macro 0.8649 / sample 0.8825, KoBERT 재현선 대비 +1.83pt.

| 런 | max_len | 훈련 시간 | micro-F1 |
| --- | --- | --- | --- |
| KoBERT 재현 | 512 | ≈10h | 0.8502 |
| exp2 (A.X) | 512 | ≈14h | 0.8601 |
| exp1 (A.X) | 8192 | ≈29h | 0.8685 |

## 즉시 할 일

**레시피는 512에서 확정하고 8192에 1회 적용한다.** 512에서 `micro_batch`를 정하고, 8192에서는 `grad_accum`으로 같은 `eff_batch`를 맞춘다(8192는 메모리 한계로 micro_batch 증설 불가 — 그래디언트 품질만 얻고 시간 이득은 없다).

### 1단계 — 배치·LR (`notebook/08_01`, 작성 지시 대기 중)

`eff_batch=8`은 long-document 축 비교를 위해 고정했던 값이다. 그 축이 끝났으므로 증설한다.

1. **배치 프로브** — `04_02`·`05_01`에 이미 있는 `probe_batches()`를 재사용하고 탐색 범위만 512 기준으로 올린다(`train_mb=(16,32,64,96)`). peak memory와 step/s를 잰다. L4에서 5~10분.
2. **LR 2런** — 프로브로 `micro_batch` 확정 후 LR 2점. 배치 8 → 32면 제곱근 스케일링 기준 3e-5 → 약 6e-5이며, 그 값과 2배 지점을 잡아 방향을 확보한다(Adam 계열에는 선형 스케일링이 과하다).

**비교 기준선**: exp2 (micro 0.8601 / macro 0.8572 / sample 0.8720, 14h).

### 2단계 — 손실 함수

> 계획·후보 평가·선정 순위·프로토콜은 `docs/experiments/loss-function.md`(SSOT). 아래는 착수 조건과 진단 요약.

1단계가 끝난 뒤에 착수한다. 배치·LR이 바뀌면 손실의 최적 설정도 따라 움직여 해석이 섞인다.

**겨냥할 지점은 다라벨 문서의 카디널리티다** (근거: `docs/experiments/no-train-analysis.md` A 「라벨 개수 bin」, `docs/experiments/modernbert-comparison.md` 「주 지표 환산」).

- k≥2는 문서의 15.0%지만 **양성 라벨 인스턴스의 29.4%**를 갖는다. 주 지표가 micro-F1이므로 이쪽 비율로 읽는다.
- k≥2 문서에만 오라클 카디널리티(정답 개수 `k`를 알고 상위 `k`개 선택)를 적용하면 exp1 micro가 0.8685 → **0.8848(+1.63pt)**. 랭킹을 전혀 바꾸지 않고 나오는 값이며, exp1이 KoBERT 재현선 대비 번 전체 개선(+1.83pt)에 맞먹는다. 세 모델 공통(+1.44 ~ +1.63pt)이다.
- 기제는 **다라벨 문서에 국한된 과소예측**이다 — k≥2에서 평균 예측 1.93 vs 정답 2.355, 과소예측 문서 44.3%. k=1에서는 평균 1.079로 오히려 과대예측이라 전역 캘리브레이션 문제가 아니다. 정답 라벨이 늘수록 2번째 이후 양성이 τ 아래로 눌린다.
- 이 헤드룸은 **임계값으로 닿지 않는다** — 단일 전역 τ는 문서별 카디널리티를 표현할 수 없고, global τ의 micro 오라클 헤드룸은 +0.0000 ~ +0.0008이었다. 손실이 점수 분포를 재배치해야 회수된다. 위 「함정」의 `alpha` 무력화(비대칭 처리가 γ 하나뿐)와 같은 지점을 가리킨다.
- **오라클-k는 상한이지 목표치가 아니다.** 실제 도달치는 상당히 낮을 것으로 보고, 판정은 상한 대비 회수율로 읽는다.
- 후보 평가 기준에 **양성·음성 비대칭 처리 능력**을 넣는다. 아래 조사는 ZLPR을 유일 후보로 좁혔으나 ASL(Asymmetric Loss, Ridnik et al. ICCV 2021 — γ+/γ− 분리 + 음성 확률 shifting)이 미조사로 남아 있고, 진단이 가리키는 방향과 정면으로 맞는다. 2단계 착수 전 좁은 범위로 보완 조사한다.

## 함정 (놓치기 쉬움)

- **`eval_steps`·`save_steps`가 배치 8 기준 하드코딩돼 있다**(`12618` = 202k/8의 반 에폭). 배치만 바꾸고 두면 평가 주기가 늘어나 early stopping이 무력화된다. 배치 32면 3,155로 재계산할 것.
- **exp1은 12에폭을 전부 소진했다**(`global_step 302,844` = 25,237×12). early stopping이 한 번도 발동하지 않았고, linear 스케줄이 12에폭에 맞춰 LR을 0으로 감쇠시키므로 **수렴해서가 아니라 예산이 끝나서 종료됐다.** 훈련 길이·스케줄도 검토 대상이다.
- **`warmup_ratio`는 비율이라 배치를 바꿔도 자동으로 따라온다.** `group_by_length`는 유지한다(배치가 커질수록 패딩 절감 효과가 커진다).
- **현행 손실은 BCE가 아니라 `FocalLoss(alpha=0.25, gamma=2)`다.** 게다가 `alpha`가 손실 전체에 곱해지는 상수라 클래스 균형 역할을 하지 않는다(표준 α-balanced focal은 양성·음성에 다른 가중을 준다). 문서당 음성:양성이 약 156:1인데 비대칭 처리가 사실상 γ 하나뿐이다.
- **`classifier_pooling`은 이미 `mean`이다**(A.X-Encoder-base 기본값). CLS 병목 우려는 이 모델에 해당하지 않는다.

## 손실 함수 후보 — 문헌 조사 결과

- **손실 계열은 바꾸지 않는다.** Menon et al.(NeurIPS 2019)에 따르면 비정규화 reduction(BCE/OVA 계열)은 precision에, 정규화 계열은 recall에 일관한다. 현행 Focal은 BCE의 재가중이라 같은 계열에 남는다. 대규모 다중 레이블 시스템(AttentionXML·LightXML·CascadeXML·XR-Transformer·Renee)이 모두 BCE를 쓰며, softmax와의 ablation을 보고한 논문은 없다. "softmax가 낫다"는 주장은 블로그 수준이거나 dual-encoder 검색에 한정된 결과(ICLR 2024)라 적용되지 않는다.
- **ZLPR**(arXiv 2208.02955)이 유일하게 값어치 있는 후보다. 손실만 바꾸는 소규모 변경이고 추론 비용이 없다. 원논문은 승패 카운트만 보고해 약하나, **HPT(EMNLP 2022)** 가 동료심사된 ablation을 냈다 — NYT +0.75 micro / WOS +0.71 / RCV1-V2 +0.58(macro +2.00). 원논문 실험에 **CNIPA·USPTO 특허 데이터**가 포함돼 공개 문헌 중 이 프로젝트와 가장 가까운 설정이다.
  - 단서: 보고된 이득은 라벨 카디널리티 2.0~7.6에서 측정됐고 여기는 1.2다. ZLPR은 본질적으로 랭킹 손실이라 Menon 기준의 precision-일관 계열이 아니다. **신뢰가 아니라 A/B로 돌린다.**
- **조사가 못 채운 부분**: ASL(Asymmetric Loss)과 focal 변형, 그리고 **LR 스케줄·배치·ModernBERT 파인튜닝 권고값**. 1단계에 필요한 부분이라 필요하면 좁은 범위로 다시 찾는다(팬아웃 없이 단일 조사로).

## 확정된 사실 · 정정

- **주 비교 지표는 멀티라벨 micro-F1**이다. top-1 weighted-F1과 P@1/3/5는 벤더 baseline의 레거시이며 병기용이다. `docs/experiments/kobert-baseline.md`가 "headline"이라 부르는 top-1 weighted-F1은 baseline 재현 범위에 한정된 표현이다.
- 이 정정은 **기존 결정을 바꾸지 않는다.** micro 기준으로 재확인한 global τ 오라클 헤드룸은 +0.0000 ~ +0.0008로 macro 기준(+0.03 ~ 0.19pt)보다도 작다. 문서 상대 임계값(`p ≥ α·p_max`)은 sample-F1을 +0.66 ~ 0.85pt 올리지만 **micro는 −0.23 ~ −0.52pt로 떨어뜨린다** — 주 지표 기준으로는 레버가 아니다.
- **앙상블은 채택하지 않는다.** 운영 환경이 단일 모델을 요구한다. 3모델 로짓평균이 micro를 0.8685 → 0.8752(+0.67pt)로 올리지만 추론 비용이 3배다.
- 나머지 닫힌 갈래는 `PROJECT.md` 「닫힌 갈래」 표를 따른다 — 계층 확장·임계값 정책·캘리브레이션·입력 필드 실험·long-document 추가 검증. **새 근거 없이 다시 제안하지 않는다.**

## 열린 질문

- **주 모델을 8192로 갈지 512로 갈지** — 운영 단일 모델 관점에서 추론 비용 차이가 크다. 레시피 확정 후 결정한다.
- **KLUE-RoBERTa-base 대조군**(선택 항목) — 성능이 아니라 주장 방어가 목적이다. 예산이 남을 때만 집행한다.
- 훈련 길이·LR 스케줄 형태(linear 유지 여부) — exp1이 스케줄을 소진한 사실과 함께 1단계에서 검토한다.

## 작업 규약

- 노트북 작성은 **지시가 있을 때만** 한다. 다음 노트북 번호는 `08_01`이다.
- 검증은 산출물에서 재현되게 한다 — 판정 기준은 서술 통계가 아니라 **결정 질문에 직접 답하는 양**으로 세우고(오라클·학습 곡선), `### verify` 셀에 assert와 SSOT 대조를 남긴다.
